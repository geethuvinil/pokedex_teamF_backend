import { AccessTokenGuardGuard } from './access_token_guard.guard';

describe('AccessTokenGuardGuard', () => {
  it('should be defined', () => {
    expect(new AccessTokenGuardGuard()).toBeDefined();
  });
});
